linters:
  enable:
    - gofumpt
    - nolintlint
    - paralleltest
    - revive
linters-settings:
  govet:
    disable:
      - fieldalignment
    enable-all: true
issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
output:
  sort-results: true
